import React from "react";
import Default from "@/layouts/default";

interface IHome {}

const Home: React.VFC<IHome> = (props) => {
  return (
    <Default>
      <div>fff</div>
    </Default>
  );
};

export default Home;
